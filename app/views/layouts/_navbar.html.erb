<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to 'Rails App', artists_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item <%= 'active' if current_page?(artists_path) %>">
          <%= link_to 'Artist', artists_path, class: "nav-link" %>
        </li>
        <li class="nav-item <%= 'active' if current_page?(genres_path) %>">
          <%= link_to 'Genres', genres_path, class: "nav-link" %>
        </li>
        <li class="nav-item <%= 'active' if current_page?(playlists_path) %>">
          <%= link_to 'Playlists', playlists_path, class: "nav-link" %>
        </li>
      </ul>
      <% if logged_in? %>
        <span>Hello, <%= current_user.name %></span>
        <%= link_to 'Sign Out', logout_path, method: :delete, class: "nav-link" %>
      <% else %>
        <%= link_to 'Sign In', login_path, class: "nav-link" %>
        <%= link_to 'Sign Up', signup_path, class: "nav-link" %>
      <% end %>
    </div>
  </div>
</nav>